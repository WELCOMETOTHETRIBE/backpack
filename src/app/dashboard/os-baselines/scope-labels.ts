/** Re-export from shared types so server and client can use scope labels without importing from a client component. */
export { getScopeComponentLabel, getScopeComponentLabels } from "@/types/boundary";
